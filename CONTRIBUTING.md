# Contributing

1. Create a feature branch.
2. Keep Android UI, backend, AI and docs changes modular.
3. Do not commit secrets, `local.properties`, APKs or user data.
4. Run the Android build and relevant smoke tests.
5. Use clear commits such as `Add justice journey tracking` or `Fix dark theme persistence`.
