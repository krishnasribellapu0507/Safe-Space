const stages=['Complaint Registered','Investigation','Hearing','Trial','Compensation','Rehabilitation'];function saveStage(){const stage=document.querySelector('#stage').value;SafeSpace.store.set('justice',{stage,updated:new Date().toISOString()});renderStage();SafeSpace.toast('Justice stage updated')}function renderStage(){const j=SafeSpace.store.get('justice',{stage:stages[0]});document.querySelector('#currentStage').textContent=j.stage}document.addEventListener('DOMContentLoaded',renderStage);