const supportive=["I can help you slow this down. Would you like grounding, journaling, or to contact support?","You can choose what feels manageable. We can do one small step at a time.","I can listen and help organize what you want to say. I won't diagnose you."];function sendChat(){const i=document.querySelector('#chatInput'),t=i.value.trim();if(!t)return;append('You',t);i.value='';setTimeout(()=>append('Safe Space',supportive[Math.floor(Math.random()*supportive.length)]),220)}function append(w,t){const d=document.createElement('div');d.className='card';d.innerHTML=`<b>${w}</b><p>${t.replace(/[<>]/g,'')}</p>`;document.querySelector('#chat').appendChild(d)}