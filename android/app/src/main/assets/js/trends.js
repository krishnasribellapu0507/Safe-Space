function trend(){const moods=SafeSpace.store.get('moods',[]);const recent=moods.slice(-5);const hard=recent.filter(x=>['low','worried','angry','overwhelmed'].includes(x.mood)).length;const band=hard>=3?'Support recommended':hard>=1?'Needs attention':'Stable';const why=hard?`${hard} of your recent check-ins reported difficult feelings.`:'Recent check-ins are within your usual demo pattern.';document.querySelector('#trendBand').textContent=band;document.querySelector('#trendWhy').textContent=why}document.addEventListener('DOMContentLoaded',trend);