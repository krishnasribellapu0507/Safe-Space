const {load,save}=require('../config/database');const Journal=require('../models/Journal');exports.list=(req,res)=>{res.json(load().journals.filter(x=>x.userId===req.user.id))};exports.create=(req,res)=>{const db=load(),j=Journal({...req.body,userId:req.user.id});db.journals.push(j);save(db);res.status(201).json(j)};exports.update=(req,res)=>{const db=load(),i=db.journals.findIndex(x=>x.id===req.params.id&&x.userId===req.user.id);if(i<0)return res.sendStatus(404);db.journals[i]={...db.journals[i],...req.body,id:db.journals[i].id};save(db);res.json(db.journals[i])};exports.remove=(req,res)=>{const db=load(),before=db.journals.length;db.journals=db.journals.filter(x=>!(x.id===req.params.id&&x.userId===req.user.id));save(db);res.status(before===db.journals.length?404:204).end()};