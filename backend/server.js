require('dotenv').config();const express=require('express'),cors=require('cors');const auth=require('./middleware/authMiddleware'),errors=require('./middleware/errorMiddleware');const app=express();app.use(cors());app.use(express.json());app.get('/api/health',(req,res)=>res.json({ok:true,name:'Safe Space API',mode:'demo'}));app.use('/api/auth',require('./routes/authRoutes'));app.use(auth);app.use('/api/mood',require('./routes/moodRoutes'));app.use('/api/journal',require('./routes/journalRoutes'));app.use('/api/counsellors',require('./routes/counsellorRoutes'));app.use('/api/appointments',require('./routes/appointmentRoutes'));app.use('/api/journey',require('./routes/journeyRoutes'));app.use('/api/trends',require('./routes/trendRoutes'));app.get('/api/user/profile',(req,res)=>{const db=require('./config/database').load();res.json(db.users.find(x=>x.id===req.user.id)||db.users[0])});app.put('/api/user/profile',(req,res)=>res.json({ok:true,note:'Demo profile updates are app-local in this prototype.'}));app.use(errors);const port=process.env.PORT||3000;if(require.main===module)app.listen(port,()=>console.log(`Safe Space API http://localhost:${port}`));module.exports=app;