PORT=3000
DATA_FILE=./data/demoData.json
DEMO_PASSWORD=demo123
RATE_LIMIT_PER_MINUTE=90
# CORS_ORIGIN=http://localhost:8080
# Demo defaults are for local hackathon use only. Add production secrets only in an untracked local .env.
